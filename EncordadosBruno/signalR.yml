info:
  name: signalR
  type: websocket
  seq: 9

websocket:
  url: "{{websocket}}/hub/Torneos?id=&transport=webSockets"
  message:
    type: json
    data: "{\"protocol\":\"json\",\"version\":1}\x1e\n"
  auth:
    type: bearer
    token: "{{token_encorder_carlos}}"

runtime:
  variables:
    - name: websocket
      value: https://localhost

settings:
  timeout: 0
  keepAliveInterval: 0

info:
  name: Obtener torneo por id
  type: http
  seq: 3

http:
  method: GET
  url: "{{api-url}}/api/Tournaments/1"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
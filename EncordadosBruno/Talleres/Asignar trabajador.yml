info:
  name: Asignar trabajador a torneo
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{api-url}}/api/Tournaments/add-worker/1"
  body:
    type: json
    data: |-
      {
        "WorkerUserId": "{{user_id_carlos}}",
        "Machine": "Máquina Alpha-1"
      }
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
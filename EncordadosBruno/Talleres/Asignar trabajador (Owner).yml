info:
  name: Asignar trabajador a torneo (Owner)
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{api-url}}/api/Tournaments/add-worker/01KS0Q28TEJ0SYA6JJ5H4W4CMP"
  body:
    type: json
    data: |-
      {
        "UserId": "{{user_id_carlos}}",
        "MachineName": "Máquina Alpha-1"
      }
  auth:
    type: bearer
    token: "{{token_admin}}"

runtime:
  variables:
    - name: user_id_carlos
      value: 01KRZAJVCA133EKNDN01TCBMJS

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  # Asignar Trabajador a Torneo como Owner

  Asigna un trabajador (encordador) y su máquina a un torneo.

  ## Authorization
  RequireOwnerRole

  ## HTTP Method
  `PATCH /api/Tournaments/add-worker/{id}`

  ## Parameters (Path)
  | Parámetro | Tipo | Descripción |
  |-----------|------|-------------|
  | `id` | ULID | ID del torneo (formato ULID) |

  ## Body (JSON)
  ```json
  {
    "UserId": "{{user_id_carlos}}",
    "MachineName": "Máquina Alpha-1"
  }
  ```

  ## Campos
  | Campo | Tipo | Descripción |
  |-------|------|-------------|
  | `UserId` | ULID | ID del usuario trabajador/encordador |
  | `MachineName` | string | Nombre de la máquina asignada |

  ## Responses
  - **200 OK**: `TournamentResponseDto` con trabajador asignado
  - **400 Bad Request**: Datos inválidos
  - **401 Unauthorized**: Token inválido o expirado
  - **403 Forbidden**: No tiene permisos de Owner
  - **404 Not Found**: Torneo o usuario no encontrado

  ## Test Cases
  - [ ] Asignar trabajador existente a torneo como Owner
  - [ ] Asignar trabajador que ya está asignado (409)
  - [ ] Asignar a torneo inexistente (404)
  - [ ] Asignar usuario inexistente como trabajador (404)

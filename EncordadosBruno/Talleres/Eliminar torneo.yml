info:
  name: Eliminar torneo
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{api-url}}/api/Tournaments/1"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
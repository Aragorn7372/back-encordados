info:
  name: Obtener trabajadores del torneo
  type: http
  seq: 9

http:
  method: GET
  url: "{{api-url}}/api/Tournaments/1/workers"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
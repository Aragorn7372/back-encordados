info:
  name: Obtener perfil por id
  type: http
  seq: 7

http:
  method: GET
  url: "{{api-url}}/api/User/{{user_id_juan}}"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
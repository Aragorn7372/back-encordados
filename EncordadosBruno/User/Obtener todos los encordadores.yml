info:
  name: Obtener todos los encordadores
  type: http
  seq: 4

http:
  method: GET
  url: "{{api-url}}/api/User/encorders?page=0&size=10&sortBy=name&direction=asc"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

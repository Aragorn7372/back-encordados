info:
  name: obtener perfil
  type: http
  seq: 1

http:
  method: GET
  url: "{{api-url}}/api/User/me"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

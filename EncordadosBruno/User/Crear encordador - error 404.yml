info:
  name: Crear encordador error
  type: http
  seq: 9

docs: |
  # Crear encordador - Usuario no encontrado

  Intenta crear un encordador con un ID de usuario inexistente para verificar error 404.

  ## Authorization
  RequireEncoderRole

  ## HTTP Method
  `POST /api/User/create-encoder`

  ## Test Cases
  - [ ] Crear encordador con usuario inexistente (404)

http:
  method: POST
  url: "{{api-url}}/api/User/create-encoder"
  auth:
    type: bearer
    token: "{{token_admin}}"
  body:
    type: json
    json: |
      "01KR424NQJR7CEHQW4STCQ3XXX"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
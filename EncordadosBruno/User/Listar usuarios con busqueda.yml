info:
  name: Listar usuarios con busqueda
  type: http
  seq: 10

http:
  method: GET
  url: "{{api-url}}/api/User/users/01KS0Q28TEJ0SYA6JJ5H4W4CMP?page=0&size=10"
  params:
    - name: page
      value: "0"
      type: query
    - name: size
      value: "10"
      type: query
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  # Listar usuarios con búsqueda

  Lista usuarios del torneo 1 filtrados por nombre/username.

  ## Authorization
  RequireAdminRole

  ## HTTP Method
  `GET /api/User?tournamentId=1&search={search}`

  ## Test Cases
  - [ ] Buscar por nombre parcial "Juan"
  - [ ] Buscar por username "jugador"
  - [ ] Buscar sin resultados

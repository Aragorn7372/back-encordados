info:
  name: Login Encordador Carlos
  type: http
  seq: 2

http:
  method: POST
  url: "{{api-url}}/api/Auth/signin"
  body:
    type: json
    data: |-
      {
        "Username":"carlos_encordador",
        "Password":"encorder123"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        bru.setEnvVar("token_carlos", data.token);
        bru.setEnvVar("user_id_carlos", data.user.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5


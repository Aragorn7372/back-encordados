info:
  name: Obtener cuerda por nombre
  type: http
  seq: 3

http:
  method: GET
  url: "{{api-url}}/api/Cuerdas/name/RPM"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
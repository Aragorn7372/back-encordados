info:
  name: Obtener material por id
  type: http
  seq: 2

http:
  method: GET
  url: "{{api-url}}/api/Materials/1"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
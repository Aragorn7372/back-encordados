info:
  name: Cancelar línea de pedido (Encoder)
  type: http
  seq: 10

docs: |
  # Cancelar Línea de Pedido como Encoder

  Cancela una línea de pedido existente.

  ## Authorization
  RequireEncoderRole

  ## HTTP Method
  `DELETE /api/Purchased/lineas/{lineaId}`

  ## Parameters (Path)
  | Parámetro | Tipo | Descripción |
  |-----------|------|-------------|
  | `lineaId` | string | ID de la línea de pedido |

  ## Responses
  - **204 No Content**: Línea cancelada exitosamente
  - **401 Unauthorized**: Token inválido o expirado
  - **403 Forbidden**: No tiene permisos de Encoder
  - **404 Not Found**: Línea no encontrada

http:
  method: DELETE
  url: "{{api-url}}/api/Purchased/lineas/01ARZ3NDEKTSV4RRFFQ69G1FAV"
  auth:
    type: bearer
    token: "{{token_encoder}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
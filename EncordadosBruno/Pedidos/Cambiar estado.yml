info:
  name: Cambiar estado del pedido
  type: http
  seq: 9

http:
  method: PATCH
  url: "{{api-url}}/api/Purchased/change-status/01KR42ME5P1Q2BGWW1DY11Z4TJ?status=COMPLETED"
  auth:
    type: bearer
    token: "{{token_carlos}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
info:
  name: Actualizar pedido (Encoder)
  type: http
  seq: 3

docs: |
  # Actualizar Pedido como Encoder

  Actualiza los datos de un pedido existente.

  ## Authorization
  RequireEncoderRole

  ## HTTP Method
  `PUT /api/Purchased/{id}`

  ## Parameters (Path)
  | Parámetro | Tipo | Descripción |
  |-----------|------|-------------|
  | `id` | string | ID del pedido |

  ## Body (JSON)
  ```json
  {
    "PlayerName": "jugador_juan",
    "AssignedToName": "carlos_encordador_actualizado",
    "Machine": "Máquina Beta-2",
    "Comments": "Comentarios actualizados",
    "PayStatus": "PAID"
  }
  ```

  ## Responses
  - **200 OK**: `PurchasedResponseDto` con datos actualizados
  - **400 Bad Request**: Datos inválidos
  - **401 Unauthorized**: Token inválido o expirado
  - **403 Forbidden**: No tiene permisos de Encoder
  - **404 Not Found**: Pedido no encontrado

http:
  method: PUT
  url: "{{api-url}}/api/Purchased/01ARZ3NDEKTSV4RRFFQ69G1FAV"
  body:
    type: json
    data: |-
      {
        "PlayerName": "jugador_juan",
        "AssignedToName": "carlos_encordador_actualizado",
        "Machine": "Máquina Beta-2",
        "Comments": "Comentarios actualizados",
        "PayStatus": "PAID"
      }
  auth:
    type: bearer
    token: "{{token_encoder}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
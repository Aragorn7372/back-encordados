info:
  name: Actualizar pedido
  type: http
  seq: 7

docs: |
  # Actualizar Pedido

  Actualiza los datos de un pedido existente.

  ## Authorization
  RequireAdminRole

  ## HTTP Method
  `PUT /api/Purchased/{id}`

  ## Parameters (Path)
  | Parámetro | Tipo | Descripción |
  |-----------|------|-------------|
  | `id` | string (ULID) | ID único del pedido |

  ## Body (JSON - todos los campos opcionales)
  ```json
  {
    "Machine": "Máquina Beta-2",
    "Comments": "Pedido actualizado por admin",
    "PayStatus": "PAID"
  }
  ```

  ## Campos Actualizables
  | Campo | Tipo | Descripción |
  |-------|------|-------------|
  | `Machine` | string? | Máquina a usar |
  | `Comments` | string? | Comentarios del pedido |
  | `PayStatus` | string? | Estado de pago (PENDING_PAYMENT, PAID, OVERDUE) |

  ## Responses
  - **200 OK**: `PurchasedResponseDto` con datos actualizados
  - **400 Bad Request**: Datos inválidos
  - **401 Unauthorized**: Token inválido o expirado
  - **403 Forbidden**: No tiene permisos de Admin
  - **404 Not Found**: Pedido no encontrado

  ## Test Cases
  - [ ] Actualizar máquina del pedido
  - [ ] Actualizar comentarios del pedido
  - [ ] Actualizar estado de pago
  - [ ] Intentar actualizar pedido inexistente (404)
  - [ ] Intentar sin permisos de Admin (403)

http:
  method: PUT
  url: "{{api-url}}/api/Purchased/01KR42ME5P1Q2BGWW1DY11Z4TJ"
  body:
    type: json
    data: |-
      {
        "Machine": "Máquina Beta-2",
        "Comments": "Pedido actualizado por admin",
        "PayStatus": "PAID"
      }
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
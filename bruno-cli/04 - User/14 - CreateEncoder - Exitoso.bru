meta {
  name: 14 - CreateEncoder - Exitoso
  type: http
  seq: 14
}

post {
  url: {{api-url}}/api/User/create-encoder
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token_owner}}
}

body:json {
  "{{tmp2_user_id}}"
}

script:post-response {
  test("Status 204", function() { expect(res.getStatus()).to.equal(204); });
}

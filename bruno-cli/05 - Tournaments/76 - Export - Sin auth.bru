meta {
  name: 76 - Export - Sin auth
  type: http
  seq: 76
}

get {
  url: {{api-url}}/api/excel/export/{{tournament_id}}
  body: none
  auth: inherit
}

script:post-response {
  test("Status 401", function() { expect(res.getStatus()).to.equal(401); });
}
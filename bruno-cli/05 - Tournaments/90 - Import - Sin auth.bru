meta {
  name: 93 - Import - Sin auth
  type: http
  seq: 90
}

post {
  url: {{api-url}}/api/export/import
  body: none
  auth: inherit
}

script:post-response {
  test("Status 401", function() { expect(res.getStatus()).to.equal(401); });
}

meta {
  name: 72 - ChangeAllLineasStatus - No encontrado
  type: http
  seq: 72
}

patch {
  url: {{api-url}}/api/Purchased/change-status/99999
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token_encorder}}
}

body:json {
  "COMPLETED"
}

script:post-response {
  const data = res.getBody();
  test("Status 404", function() { expect(res.getStatus()).to.equal(404); });
  test("Mensaje de error", function() { expect(data).to.have.property("error"); });
}

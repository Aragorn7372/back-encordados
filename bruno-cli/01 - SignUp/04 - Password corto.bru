meta {
  name: 04 - Password corto
  type: http
  seq: 4
}

post {
  url: {{api-url}}/api/Auth/signup
  body: json
  auth: inherit
}

body:json {
  {
    "Username": "test_password_corto",
    "Email": "test_password_corto@test.com",
    "Password": "abc"
  }
}

script:post-response {
  test("Status 400 Bad Request", function() { expect(res.getStatus()).to.equal(400); });
}
